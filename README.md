# OpenTronsProtocolGenerator
OpenTrons OT-2 protocol generator

# バックエンドの起動

バックエンドディレクトリに移動する。

```bash
cd backend
```

VENVのセットアップ

```bash
python -m venv venv
```

```bash
source venv/bin/activate
```


